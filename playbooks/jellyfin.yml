---
- name: Déploiement Jellyfin + dépendances NAS
  hosts: bazzite_hosts
  gather_facts: true

  vars:
    ansible_user: "famille"   # à adapter si besoin
    jellyfin_config_dir: "{{ ansible_user_dir }}/.local/share/jellyfin/config"
    jellyfin_cache_dir: "{{ ansible_user_dir }}/.local/share/jellyfin/cache"
    container_dest: "{{ ansible_user_dir }}/.config/containers/systemd/jellyfin.container"
    
    selinux_paths:
      - "{{ ansible_user_dir }}/internes/exos/Videos"

    # Montages NAS nécessaires pour Jellyfin
    required_mounts:
      - "{{ (ansible_user_dir ~ '/nas/video') | regex_replace('^/', '') | regex_replace('/', '-') }}.mount"
      - "{{ (ansible_user_dir ~ '/nas/PlexMediaServer') | regex_replace('^/', '') | regex_replace('/', '-') }}.mount"

  tasks:

    - name: Créer répertoires Jellyfin
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
        owner: "{{ ansible_user }}"
        group: "{{ ansible_user }}"
      loop:
        - "{{ jellyfin_config_dir }}"
        - "{{ jellyfin_cache_dir }}"

    - name: Créer répertoire systemd user
      ansible.builtin.file:
        path: "{{ ansible_user_dir }}/.config/containers/systemd"
        state: directory
        mode: "0755"
        owner: "{{ ansible_user }}"
        group: "{{ ansible_user }}"

    - name: Appliquer contexte SELinux permanent
      community.general.sefcontext:
        target: "{{ item }}(/.*)?"
        setype: container_file_t
        state: present
      loop: "{{ selinux_paths }}"
      become: true

    - name: restorecon immédiat
      ansible.builtin.command: "restorecon -Rv {{ item }}"
      loop: "{{ selinux_paths }}"
      become: true
      changed_when: false

    # === Déploiement du .container ===
    - name: Déployer jellyfin.container
      ansible.builtin.template:
        src: jellyfin.container.j2
        dest: "{{ container_dest }}"
        owner: "{{ ansible_user }}"
        group: "{{ ansible_user }}"
        mode: "0644"
      notify: Recharger systemd user

    - name: Recharger systemd user
      ansible.builtin.systemd:
        daemon_reload: true
        scope: user
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid | default(1000) }}"
      notify: Redémarrer Jellyfin

    - name: Démarrer Jellyfin
      ansible.builtin.systemd:
        name: jellyfin.service
        state: started
        enabled: true
        scope: user
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid | default(1000) }}"

  handlers:
    - name: Recharger systemd user
      ansible.builtin.systemd:
        daemon_reload: true
        scope: user
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid | default(1000) }}"

    - name: Redémarrer Jellyfin
      ansible.builtin.systemd:
        name: jellyfin.service
        state: restarted
        scope: user
      environment:
        XDG_RUNTIME_DIR: "/run/user/{{ ansible_user_uid | default(1000) }}"